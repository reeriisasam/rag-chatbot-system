# 🚀 Quick Start Guide

คู่มือเริ่มต้นใช้งานอย่างรวดเร็ว

## ⚡ การติดตั้งแบบเร็ว (5 นาที)

### 1. Setup โปรเจค

```bash
# Clone และเข้า directory
cd rag-chatbot-system

# สร้าง virtual environment
python -m venv venv

# Activate virtual environment
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# ติดตั้ง dependencies
pip install -r requirements.txt

# สร้างโครงสร้าง folder
python setup_project.py
```

### 2. ตั้งค่า API Key

#### Option A: ใช้ OpenAI

```bash
# สร้างไฟล์ .env
cp .env.example .env

# แก้ไข .env และใส่ API key
OPENAI_API_KEY=sk-your-actual-api-key-here
```

#### Option B: ใช้ Ollama (ฟรี, รันบน local)

```bash
# ติดตั้ง Ollama
curl -fsSL https://ollama.com/install.sh | sh

# Pull model
ollama pull llama2

# รัน Ollama server (เปิด terminal ใหม่)
ollama serve

# แก้ไข config.yaml
# เปลี่ยน provider เป็น "custom"
# เปลี่ยน base_url เป็น "http://localhost:11434/v1"
```

### 3. รันโปรแกรม

```bash
python main.py
```

## 📝 ตัวอย่างการใช้งาน

### สถานการณ์ 1: Chat ธรรมดา

```
🤖 > ยินดีต้อนรับ!

👤 คุณ: สวัสดีครับ
🤖 Assistant: สวัสดีครับ! มีอะไรให้ช่วยไหมครับ?

👤 คุณ: วันนี้อากาศเป็นอย่างไร
🤖 Assistant: ผมไม่สามารถเช็คอากาศได้โดยตรง แต่คุณสามารถถามเกี่ยวกับข้อมูลในเอกสารที่ฉันมีได้ครับ
```

### สถานการณ์ 2: ถามจากเอกสาร

```
👤 คุณ: มีข้อมูลอะไรบ้างในเอกสาร
🤖 Assistant: ในเอกสารมีข้อมูลเกี่ยวกับ...
  📚 ใช้ข้อมูลจาก RAG
  📄 แหล่งที่มา: sample.txt

👤 คุณ: อธิบาย RAG หน่อย
🤖 Assistant: RAG หรือ Retrieval Augmented Generation คือเทคนิคที่...
  📚 ใช้ข้อมูลจาก RAG
```

### สถานการณ์ 3: ใช้โหมดเสียง

```
👤 คุณ: /mode voice
✓ เปลี่ยนเป็นโหมดเสียงแล้ว

🎤 กำลังฟัง... (พูดได้เลย)
[คุณพูด: "สวัสดีครับ"]

👤 คุณ: สวัสดีครับ
🤖 Assistant: สวัสดีครับ! มีอะไรให้ช่วยไหมครับ?
🔊 [ระบบพูดคำตอบออกมา]
```

## 🎯 Use Cases

### 1. Customer Support Bot

```yaml
# วางไฟล์ FAQ, คู่มือผลิตภัณฑ์ใน data/documents/
data/documents/
  ├── faq.pdf
  ├── product_manual.docx
  └── troubleshooting.txt
```

### 2. Knowledge Base Search

```yaml
# วางเอกสารองค์กร, policy ต่างๆ
data/documents/
  ├── company_policy.pdf
  ├── hr_handbook.pdf
  └── it_guidelines.md
```

### 3. Educational Assistant

```yaml
# วางเอกสารการเรียน, หนังสือ
data/documents/
  ├── chapter1.pdf
  ├── chapter2.pdf
  └── notes.txt
```

## 🔧 Configuration Tips

### ปรับแต่งการตอบคำถาม

```yaml
# config.yaml
rag:
  top_k: 5                    # จำนวน documents ที่จะดึงมา
  similarity_threshold: 0.7   # คะแนนความคล้ายขั้นต่ำ
  chunk_size: 1000           # ขนาดของแต่ละ chunk
  chunk_overlap: 200          # overlap ระหว่าง chunks
```

### ปรับแต่ง LLM

```yaml
llm:
  temperature: 0.7    # ความ creative (0-1)
  max_tokens: 2000    # ความยาวคำตอบสูงสุด
```

### ปรับแต่งเสียง

```yaml
audio:
  tts:
    rate: 150      # ความเร็วในการพูด (50-300)
    volume: 0.9    # ระดับเสียง (0.0-1.0)
```

## ❓ FAQ

### Q: ระบบใช้ RAM เยอะไหม?

**A:** ขึ้นอยู่กับ LLM ที่เลือก
- OpenAI API: ใช้ RAM น้อย (~500MB)
- Llama Local: ใช้ RAM 4-8GB ขึ้นไป

### Q: รองรับภาษาไทยไหม?

**A:** รองรับทั้งภาษาไทยและอังกฤษ
- OpenAI: รองรับดี
- Llama2: รองรับได้แต่อาจไม่แม่นเท่า
- Whisper: รองรับภาษาไทยได้ดี

### Q: ใช้เอกสารได้กี่ไฟล์?

**A:** ไม่จำกัด แต่ขึ้นอยู่กับ:
- RAM ของเครื่อง
- ขนาดของ vector database
- แนะนำ: เริ่มจาก 10-20 ไฟล์

### Q: ต้องมี GPU ไหม?

**A:** ไม่จำเป็น
- OpenAI API: ไม่ต้องใช้ GPU
- Llama Local: GPU จะช่วยให้เร็วขึ้น แต่ CPU ก็ใช้ได้

## 🐛 แก้ปัญหาเบื้องต้น

### ปัญหา: ติดตั้ง PyAudio ไม่ได้

```bash
# Windows
pip install pipwin
pipwin install pyaudio

# Linux
sudo apt-get install portaudio19-dev python3-pyaudio
pip install pyaudio

# macOS
brew install portaudio
pip install pyaudio
```

### ปัญหา: Microphone ไม่ทำงาน

```bash
# ทดสอบ microphone
python -c "import speech_recognition as sr; r = sr.Recognizer(); print('Testing...'); m = sr.Microphone(); print('OK!')"
```

### ปัญหา: OpenAI API Error

```bash
# ตรวจสอบ API key
python -c "import os; from dotenv import load_dotenv; load_dotenv(); print(os.getenv('OPENAI_API_KEY'))"

# ควรเห็น API key (เริ่มด้วย sk-)
```

### ปัญหา: Memory Error (Llama)

```yaml
# ลด chunk_size ใน config.yaml
rag:
  chunk_size: 500  # ลดจาก 1000
  top_k: 3         # ลดจาก 5
```

## 📚 เพิ่มเติม

- [README.md](README.md) - คู่มือฉบับเต็ม
- [config.yaml](config.yaml) - การตั้งค่าทั้งหมด
- [เอกสาร LangChain](https://python.langchain.com/)
- [เอกสาร LangGraph](https://langchain-ai.github.io/langgraph/)

## 💡 Tips

1. **เริ่มจาก OpenAI** - ง่ายและเร็วที่สุด
2. **ทดสอบกับเอกสารน้อยๆ ก่อน** - แล้วค่อยเพิ่ม
3. **ใช้โหมดข้อความก่อน** - stable กว่าโหมดเสียง
4. **อ่าน logs** - ดูที่ `logs/app.log` เมื่อมีปัญหา

---

Happy chatting! 🎉